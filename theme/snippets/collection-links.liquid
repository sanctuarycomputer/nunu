{% assign collection_links_intro_text = shop.metafields.accentuate.collection_links_intro_text %}

<div class="debug CollectionLinks site-padding-x serif-sm-md mt1_5">
  {{ collection_links_intro_text }}
  {% assign selected_collections = collection_links | split: '|' %}
  {% for selected_collection in selected_collections %}
    {% if forloop.last and forloop.length > 1 %}or{% endif %}
    <a class="CollectionLinks__button button--style-rounded-border-xxs" href="{{ collections[selected_collection].url }}" aria-label={{ 'snippets.collection-links.link.aria-label' | t }}>{{ collections[selected_collection].title }}</a>
  {% endfor %}
</div>
